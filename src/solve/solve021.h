#pragma once
#include "euler.h"

uint32_t amicable_numbers(size_t max);
uint32_t amicable_pair(uint32_t num);

struct euler_result solve021(json_t *input);
extern const struct euler_input input021[];
